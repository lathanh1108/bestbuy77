.product-tile(data-prodid=product.id)
    .content
        a(href="/store/detail/" + product.id).image
            img(src=product.thumbnail, alt="product.title")
        .name
            a(href="/store/detail/" + product.id) #{product.title}
        .description
            p= product.description
        .price
            p= __('global.price_unit') + product.price
        .action
            button(type="button" data-prodid=product.id class="btn btn-block btn-add-to-cart") #{__('product_tile.add_to_cart')}